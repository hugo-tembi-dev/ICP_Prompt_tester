{
  "name": "icp-prompt-tester",
  "version": "1.0.0",
  "description": "Interface for creating custom prompts through Q&A pairs and testing them against JSON analysis",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["prompt", "testing", "json", "analysis", "qa"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
